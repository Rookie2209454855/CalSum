// 食物数据库 - 每100g的热量(kcal)
export const foodDatabase = [];

// 根据食物名称查找热量 - 始终返回null，因为不再有预设数据
export function getCaloriesPer100g(foodName) {
  return null;
}

// 计算指定重量的食物热量 - 始终返回null，因为没有预设数据
export function calculateCalories(foodName, weightInGrams) {
  return null;
}
